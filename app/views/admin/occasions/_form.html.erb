<%= form_for([:admin, occasion]) do |form| %>
  <%= render 'errors' %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div><br>

  <div class="actions"><br>
    <%= form.submit :class => 'btn btn-primary' %>
  </div>
<% end %><br/>
