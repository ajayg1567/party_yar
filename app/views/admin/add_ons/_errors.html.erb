<% if @add_on.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@add_on.errors.count, "error") %> prohibited this product from being saved:</h2>

	<ul>
		<% @add_on.errors.full_messages.each do |message| %>
		<li><%= message %></li>
		<% end %>
	</ul>
</div>
<% end %>